# Favorite P1（端侧收藏补完）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 首页"收藏"按钮与收藏页接真数据：toggle 入库、收藏页按时间倒序展示，全程离线可用、零服务端改动。

**Architecture:** 新增 `FavoriteDao`（复用既有 `favorite` 表，不升 Room 版本）；`QuoteRepository` 加 `observeFavorites()`，`toggleFavorite` 真实现；`FavoriteViewModel` 去掉 `filter { false }`。单元测试 mock DAO（TDD 红绿），另附 androidTest 真机验证文件。

**Tech Stack:** Kotlin + Room 2.8.4 + Hilt 2.60.1 + JUnit4 + MockK + kotlinx-coroutines-test + Turbine 无（用 `first { }`）。

---

## File Structure

```
app/src/main/java/com/dailymind/core/database/dao/
  FavoriteDao.kt                  # 新增：upsert/delete/observeIds/getIdsOnce
app/src/main/java/com/dailymind/core/database/
  DailyMindDatabase.kt            # 加 abstract fun favoriteDao()
app/src/main/java/com/dailymind/core/database/di/
  DatabaseModule.kt               # 加 provideFavoriteDao
app/src/main/java/com/dailymind/core/data/
  QuoteRepository.kt              # 加 observeFavorites()
  QuoteRepositoryImpl.kt          # 构造器加 FavoriteDao；toggle/observe 真实现
app/src/main/java/com/dailymind/feature/favorite/
  FavoriteViewModel.kt            # 接 observeFavorites()
app/src/test/java/com/dailymind/core/data/
  QuoteRepositoryTest.kt          # 加 toggle/observe 单测
app/src/test/java/com/dailymind/feature/favorite/
  FavoriteViewModelTest.kt        # 新增：透出 repo 流
app/src/androidTest/java/com/dailymind/core/database/
  FavoriteDaoTest.kt              # 新增：真机/模拟器验证（本机无设备时只编译）
```

---

### Task 1: FavoriteDao + 数据库接线

**Files:**
- Create: `app/src/main/java/com/dailymind/core/database/dao/FavoriteDao.kt`
- Modify: `app/src/main/java/com/dailymind/core/database/DailyMindDatabase.kt`
- Modify: `app/src/main/java/com/dailymind/core/database/di/DatabaseModule.kt`
- Create: `app/src/androidTest/java/com/dailymind/core/database/FavoriteDaoTest.kt`

- [ ] **Step 1: Write the DAO and wiring (no behavior change yet)**

```kotlin
// app/src/main/java/com/dailymind/core/database/dao/FavoriteDao.kt
package com.dailymind.core.database.dao

import androidx.room.Dao
import androidx.room.Query
import androidx.room.Upsert
import com.dailymind.core.database.entity.FavoriteEntity
import com.dailymind.core.database.entity.QuoteEntity
import kotlinx.coroutines.flow.Flow

@Dao
interface FavoriteDao {
    @Upsert
    suspend fun upsert(entry: FavoriteEntity)

    @Query("DELETE FROM favorite WHERE quoteId = :quoteId")
    suspend fun deleteById(quoteId: String)

    @Query("SELECT quote.* FROM quote INNER JOIN favorite ON quote.id = favorite.quoteId WHERE quote.deletedAt IS NULL ORDER BY favorite.createdAt DESC")
    fun observeFavorites(): Flow<List<QuoteEntity>>

    @Query("SELECT quoteId FROM favorite")
    suspend fun getIdsOnce(): List<String>
}
```

```kotlin
// DailyMindDatabase.kt: import 加一行，类内加一行
import com.dailymind.core.database.dao.FavoriteDao
import com.dailymind.core.database.dao.HistoryDao
import com.dailymind.core.database.dao.QuoteDao
```
类体内（`quoteDao()` 与 `historyDao()` 并列）加：
```kotlin
    abstract fun favoriteDao(): FavoriteDao
```

```kotlin
// DatabaseModule.kt: import 加一行，末尾加 provider
import com.dailymind.core.database.dao.FavoriteDao
```
```kotlin
    @Provides
    fun provideFavoriteDao(db: DailyMindDatabase): FavoriteDao = db.favoriteDao()
```

```kotlin
// app/src/androidTest/java/com/dailymind/core/database/FavoriteDaoTest.kt
package com.dailymind.core.database

import androidx.room.Room
import androidx.test.core.app.ApplicationProvider
import androidx.test.ext.junit.runners.AndroidJUnit4
import com.dailymind.core.database.entity.FavoriteEntity
import com.dailymind.core.database.entity.QuoteEntity
import kotlinx.coroutines.flow.first
import kotlinx.coroutines.test.runTest
import org.junit.Assert.*
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class FavoriteDaoTest {
    private fun db() = Room.inMemoryDatabaseBuilder(
        ApplicationProvider.getApplicationContext(),
        DailyMindDatabase::class.java
    ).allowMainThreadQueries().build()

    @Test fun toggleRoundTrip() = runTest {
        val db = db()
        db.quoteDao().upsertAll(listOf(QuoteEntity("1", "Hello", "你好", "Anon", "life", 1, null, null, 1000L, null)))
        db.favoriteDao().upsert(FavoriteEntity("1", 2000L))
        assertEquals(listOf("1"), db.favoriteDao().getIdsOnce())
        assertEquals("Hello", db.favoriteDao().observeFavorites().first()[0].content)
        db.favoriteDao().deleteById("1")
        assertTrue(db.favoriteDao().getIdsOnce().isEmpty())
        db.close()
    }
}
```

- [ ] **Step 2: Run compile to verify it builds**

Run: `server\gradlew.bat :app:compileDebugKotlin :app:compileDebugAndroidTestKotlin`
Expected: BUILD SUCCESSFUL（androidTest 只编译，真机验证待有设备后跑 `:app:connectedDebugAndroidTest`）

- [ ] **Step 3: Commit**

```bash
git add app/src/main/java/com/dailymind/core/database/dao/FavoriteDao.kt app/src/main/java/com/dailymind/core/database/DailyMindDatabase.kt app/src/main/java/com/dailymind/core/database/di/DatabaseModule.kt app/src/androidTest/java/com/dailymind/core/database/FavoriteDaoTest.kt
git commit -m "feat(app): add FavoriteDao with database wiring"
```

---

### Task 2: Repository toggle + observeFavorites（TDD）

**Files:**
- Modify: `app/src/main/java/com/dailymind/core/data/QuoteRepository.kt`
- Modify: `app/src/main/java/com/dailymind/core/data/QuoteRepositoryImpl.kt`
- Modify: `app/src/test/java/com/dailymind/core/data/QuoteRepositoryTest.kt`（末尾追加；文件已有 `repo()` helper，签名改为含 `favorites` 参数见 Step 1）

- [ ] **Step 1: Write the failing tests**

`QuoteRepositoryTest.kt` 的 `repo()` helper 改为：
```kotlin
    private fun repo(
        dao: QuoteDao = mockk(relaxed = true),
        api: ApiService = mockk(),
        historyDao: HistoryDao = mockk(relaxed = true),
        store: DailyQuoteStore = mockk(relaxed = true),
        favorites: FavoriteDao = mockk(relaxed = true)
    ) = QuoteRepositoryImpl(dao, api, historyDao, store, favorites)
```
文件末尾（`getRandomQuote falls back to cache on network error` 之后）追加：
```kotlin
    @Test fun `toggleFavorite adds when absent`() = runTest {
        val favorites = mockk<FavoriteDao>(relaxed = true)
        coEvery { favorites.getIdsOnce() } returns emptyList()
        repo(favorites = favorites).toggleFavorite("9")
        coVerify { favorites.upsert(match<FavoriteEntity> { it.quoteId == "9" }) }
    }

    @Test fun `toggleFavorite removes when present`() = runTest {
        val favorites = mockk<FavoriteDao>(relaxed = true)
        coEvery { favorites.getIdsOnce() } returns listOf("9")
        repo(favorites = favorites).toggleFavorite("9")
        coVerify { favorites.deleteById("9") }
        coVerify(exactly = 0) { favorites.upsert(any()) }
    }

    @Test fun `observeFavorites emits joined quotes`() = runTest {
        val favorites = mockk<FavoriteDao>()
        coEvery { favorites.observeFavorites() } returns flowOf(listOf(entity))
        val list = repo(favorites = favorites).observeFavorites().first()
        assertEquals(listOf("9"), list.map { it.id })
    }
```
imports 追加（按字母序插入现有 import 块）：
```kotlin
import com.dailymind.core.database.dao.FavoriteDao
import com.dailymind.core.database.entity.FavoriteEntity
```

- [ ] **Step 2: Run test to verify it fails**

Run: `server\gradlew.bat :app:testDebugUnitTest --tests "com.dailymind.core.data.QuoteRepositoryTest"`
Expected: FAIL 编译错误 `No value passed for parameter 'favorites'`（构造器还未加参）

- [ ] **Step 3: Write minimal implementation**

`QuoteRepository.kt` 接口加一行：
```kotlin
    fun observeFavorites(): Flow<List<Quote>>
```

`QuoteRepositoryImpl.kt`：
```kotlin
import com.dailymind.core.database.dao.FavoriteDao
import com.dailymind.core.database.entity.FavoriteEntity
```
构造器加参：
```kotlin
class QuoteRepositoryImpl @Inject constructor(
    private val dao: QuoteDao,
    private val api: ApiService,
    private val historyDao: HistoryDao,
    private val dailyStore: DailyQuoteStore,
    private val favorites: FavoriteDao
) : QuoteRepository {
```
`toggleFavorite` 替换 stub：
```kotlin
    override suspend fun toggleFavorite(quoteId: String) {
        if (favorites.getIdsOnce().contains(quoteId)) {
            favorites.deleteById(quoteId)
        } else {
            favorites.upsert(FavoriteEntity(quoteId, System.currentTimeMillis()))
        }
    }
```
`observeQuotes()` 之后加：
```kotlin
    override fun observeFavorites(): Flow<List<Quote>> =
        favorites.observeFavorites().map { list -> list.map { it.toModel() } }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `server\gradlew.bat :app:testDebugUnitTest --tests "com.dailymind.core.data.QuoteRepositoryTest"`
Expected: BUILD SUCCESSFUL，7 个旧测试 + 3 个新测试全过

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/dailymind/core/data/QuoteRepository.kt app/src/main/java/com/dailymind/core/data/QuoteRepositoryImpl.kt app/src/test/java/com/dailymind/core/data/QuoteRepositoryTest.kt
git commit -m "feat(app): wire real favorite toggle and list"
```

---

### Task 3: FavoriteViewModel 接真数据流

**Files:**
- Modify: `app/src/main/java/com/dailymind/feature/favorite/FavoriteViewModel.kt`
- Create: `app/src/test/java/com/dailymind/feature/favorite/FavoriteViewModelTest.kt`

- [ ] **Step 1: Write the failing test**

```kotlin
// app/src/test/java/com/dailymind/feature/favorite/FavoriteViewModelTest.kt
package com.dailymind.feature.favorite

import com.dailymind.MainDispatcherRule
import com.dailymind.core.data.QuoteRepository
import com.dailymind.core.model.Quote
import io.mockk.every
import io.mockk.mockk
import kotlinx.coroutines.flow.flowOf
import kotlinx.coroutines.test.runTest
import org.junit.Assert.*
import org.junit.Rule
import org.junit.Test

class FavoriteViewModelTest {
    @get:Rule val mainDispatcherRule = MainDispatcherRule()

    @Test fun `emits favorites from repository`() = runTest {
        val repo = mockk<QuoteRepository>()
        every { repo.observeFavorites() } returns flowOf(
            listOf(Quote("1", "Hello", "你好", "Anon", "life", 1, null, null, 1000L))
        )
        val vm = FavoriteViewModel(repo)
        assertEquals("Hello", vm.favorites.first { it.isNotEmpty() }[0].content)
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `server\gradlew.bat :app:testDebugUnitTest --tests "com.dailymind.feature.favorite.FavoriteViewModelTest"`
Expected: FAIL 编译错误 `Unresolved reference 'observeFavorites'`（接口还未加——若 Task 2 已合入则改为 FAIL 断言 `first { }` 超时/空列表，因为 ViewModel 仍返回空流）

- [ ] **Step 3: Write minimal implementation**

```kotlin
// app/src/main/java/com/dailymind/feature/favorite/FavoriteViewModel.kt
package com.dailymind.feature.favorite

import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import com.dailymind.core.data.QuoteRepository
import com.dailymind.core.model.Quote
import dagger.hilt.android.lifecycle.HiltViewModel
import kotlinx.coroutines.flow.SharingStarted
import kotlinx.coroutines.flow.StateFlow
import kotlinx.coroutines.flow.stateIn
import javax.inject.Inject

@HiltViewModel
class FavoriteViewModel @Inject constructor(
    repo: QuoteRepository
) : ViewModel() {
    val favorites: StateFlow<List<Quote>> = repo.observeFavorites()
        .stateIn(viewModelScope, SharingStarted.WhileSubscribed(5000), emptyList())
}
```
（即删除 `.map { it.filter { _ -> false } }` 一行及其 import。）

- [ ] **Step 4: Run test to verify it passes**

Run: `server\gradlew.bat :app:testDebugUnitTest --tests "com.dailymind.feature.favorite.FavoriteViewModelTest"`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: Commit**

```bash
git add app/src/main/java/com/dailymind/feature/favorite/FavoriteViewModel.kt app/src/test/java/com/dailymind/feature/favorite/FavoriteViewModelTest.kt
git commit -m "feat(app): show real favorites in FavoriteViewModel"
```

---

### Task 4: 全量回归 + 手动验证

**Files:** 无新增，仅验证。

- [ ] **Step 1: 全量单元测试**

Run: `server\gradlew.bat :app:test`
Expected: BUILD SUCCESSFUL（debug + release 单元测试全绿）

- [ ] **Step 2: 真机手动验证（有设备时）**

Run: 安装 debug 包 → 首页点 "+ Favorite" → 切到 Saved 页看到该条 → 回首页再点一次取消 → Saved 页变空态 "No saved quotes yet" → 杀进程重进，收藏仍在（Room 持久化）
Expected: 与描述一致；杀进程后仍在

- [ ] **Step 3: Commit（若有修正，无则跳过）**

```bash
git status --short -- app/
```

---

## Self-Review

- Spec coverage: §6 P1 三项（FavoriteDao/toggle/observeFavorites + ViewModel + TDD + `:app:test`）→ Task 1/2/3/4 全覆盖；P2 内容本 plan 不含（独立 plan）。
- Placeholder scan: 无 TBD/TODO；每步含完整代码与精确命令；androidTest 需设备的限制已在 Task 1 Step 2 注明。
- Type consistency: `FavoriteDao(upsert/deleteById/observeFavorites/getIdsOnce)` 在 Task 1 定义、Task 2/3 与测试中同名同参；`FavoriteEntity(quoteId, createdAt)` 与既有 entity 一致；`observeFavorites(): Flow<List<Quote>>` 在接口/实现/ViewModel/测试四处一致；`repo()` helper 新签名在 Task 2 Step 1 一次改全（含旧调用点 `observeQuotes`/`getRandomQuote` 测试走默认值，无需再改）。
